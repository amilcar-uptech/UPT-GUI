import { DependenciesPipe } from './dependencies.pipe';

describe('DependenciesPipe', () => {
  it('create an instance', () => {
    const pipe = new DependenciesPipe();
    expect(pipe).toBeTruthy();
  });
});
