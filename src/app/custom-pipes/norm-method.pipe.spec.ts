import { NormMethodPipe } from './norm-method.pipe';

describe('NormMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new NormMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
