import { SquareKilometersPipe } from './square-kilometers.pipe';

describe('SquareKilometersPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareKilometersPipe();
    expect(pipe).toBeTruthy();
  });
});
